{
  "name": "automatic-takeoffs",
  "version": "1.0.0",
  "description": "Runs the Template matching process",
  "main": " server.js",
  "scripts": {
    "start": "node server.js & cd clientApp && npm start",
    "build": "cd clientApp && npm run build && mv ./build ../build"
  },
  "author": "Joe Ronaldson",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "opencv4nodejs": "^5.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "engines": {
    "node": "14.18.1"
  }
}
