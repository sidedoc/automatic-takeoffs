{
  "name": "automatic-takeoffs",
  "version": "1.0.0",
  "description": "Runs the Template matching process",
  "main": "server.js",
  "scripts": {
    "start": "NODE_ENV=local node server.js & cd clientApp && npm start",
    "build": "cd clientApp && NODE_ENV=docker npm run build && mv ./build ../build"
  },
  "author": "Joe Ronaldson",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "@u4/opencv4nodejs": "^6.5.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
